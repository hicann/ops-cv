/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

#include <iostream>
#include <gtest/gtest.h>
#include "tiling_context_faker.h"
#include "tiling_case_executor.h"
#include "../../../../op_kernel/arch35/yolox_bounding_box_decode_tiling_data.h"

using namespace std;
using namespace ge;

class YoloxBoundingBoxDecodeTiling : public testing::Test {
protected:
    static void SetUpTestCase() { std::cout << "YoloxBoundingBoxDecodeTiling SetUp" << std::endl; }

    static void TearDownTestCase() { std::cout << "YoloxBoundingBoxDecodeTiling TearDown" << std::endl; }
};

std::map<std::string, std::string> soc_version_infos = {{"Short_SoC_version", "Ascend950"}};

constexpr size_t YOLOX_BBOX_DECODE_SYS_WORKSPACE_SIZE = 16777216;
constexpr uint64_t CORE_NUM = 64;
constexpr uint64_t UB_SIZE = 262144;
constexpr uint64_t MAX_TILING_DATA_SIZE = 4096;

// Test case 1: regular shape, float32, boxCount=32*4=128 < PER_CORE_MIN(1024), needCoreNum=1
TEST_F(YoloxBoundingBoxDecodeTiling, yolox_bounding_box_decode_fp32_regular)
{
    struct YoloxBoundingBoxDecodeCompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "YoloxBoundingBoxDecode",
        {
            {{{4, 4}, {4, 4}}, ge::DT_FLOAT, ge::FORMAT_ND},         // input priors: (N, 4)
            {{{32, 4, 4}, {32, 4, 4}}, ge::DT_FLOAT, ge::FORMAT_ND}, // input bboxes: (B, N, 4)
        },
        {
            {{{32, 4, 4}, {32, 4, 4}}, ge::DT_FLOAT, ge::FORMAT_ND}, // output decoded_bboxes: (B, N, 4)
        },
        {
            /* attrs */
        },
        &compileInfo, "Ascend950",
        CORE_NUM, // number of cores obtained in the tiling phase
        UB_SIZE,  // the ubsize obtained in the tiling phase, but the actual obtained value is 256 bytes less than the
                  // specified value
        MAX_TILING_DATA_SIZE);    // specifies the maximum size of the tiling data in the tiling phase
    uint64_t expectTilingKey = 0; // YOLOX_BBOX_DECODE_SCH_MODE_BOX
    // boxCount=B*N=128, boxNum=N=4, needCoreNum=max(ceil(128/1024),1)=1
    string expectTilingData = "128 4 1 ";
    std::vector<size_t> expectWorkspaces = {YOLOX_BBOX_DECODE_SYS_WORKSPACE_SIZE};
    ExecuteTestCase(tilingContextPara, ge::GRAPH_SUCCESS, expectTilingKey, expectTilingData, expectWorkspaces);
}

// Test case 2: regular shape, float16, same tiling data as float32
TEST_F(YoloxBoundingBoxDecodeTiling, yolox_bounding_box_decode_fp16_regular)
{
    struct YoloxBoundingBoxDecodeCompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "YoloxBoundingBoxDecode",
        {
            {{{4, 4}, {4, 4}}, ge::DT_FLOAT16, ge::FORMAT_ND},         // input priors: (N, 4)
            {{{32, 4, 4}, {32, 4, 4}}, ge::DT_FLOAT16, ge::FORMAT_ND}, // input bboxes: (B, N, 4)
        },
        {
            {{{32, 4, 4}, {32, 4, 4}}, ge::DT_FLOAT16, ge::FORMAT_ND}, // output decoded_bboxes: (B, N, 4)
        },
        {
            /* attrs */
        },
        &compileInfo, "Ascend950",
        CORE_NUM, // number of cores obtained in the tiling phase
        UB_SIZE,  // the ubsize obtained in the tiling phase, but the actual obtained value is 256 bytes less than the
                  // specified value
        MAX_TILING_DATA_SIZE);    // specifies the maximum size of the tiling data in the tiling phase
    uint64_t expectTilingKey = 0; // YOLOX_BBOX_DECODE_SCH_MODE_BOX
    // boxCount=B*N=128, boxNum=N=4, needCoreNum=1
    string expectTilingData = "128 4 1 ";
    std::vector<size_t> expectWorkspaces = {YOLOX_BBOX_DECODE_SYS_WORKSPACE_SIZE};
    ExecuteTestCase(tilingContextPara, ge::GRAPH_SUCCESS, expectTilingKey, expectTilingData, expectWorkspaces);
}

// Test case 3: middle size shape, per-core lower bound protection kicks in, 10 cores
TEST_F(YoloxBoundingBoxDecodeTiling, yolox_bounding_box_decode_multi_core)
{
    struct YoloxBoundingBoxDecodeCompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "YoloxBoundingBoxDecode",
        {
            {{{10000, 4}, {10000, 4}}, ge::DT_FLOAT, ge::FORMAT_ND},       // input priors: (N, 4)
            {{{1, 10000, 4}, {1, 10000, 4}}, ge::DT_FLOAT, ge::FORMAT_ND}, // input bboxes: (B, N, 4)
        },
        {
            {{{1, 10000, 4}, {1, 10000, 4}}, ge::DT_FLOAT, ge::FORMAT_ND}, // output decoded_bboxes: (B, N, 4)
        },
        {
            /* attrs */
        },
        &compileInfo, "Ascend950",
        CORE_NUM, // number of cores obtained in the tiling phase
        UB_SIZE,  // the ubsize obtained in the tiling phase, but the actual obtained value is 256 bytes less than the
                  // specified value
        MAX_TILING_DATA_SIZE);    // specifies the maximum size of the tiling data in the tiling phase
    uint64_t expectTilingKey = 0; // YOLOX_BBOX_DECODE_SCH_MODE_BOX
    // boxCount=1*10000=10000, boxNum=10000, perCoreBoxes=max(ceil(10000/64),1024)=1024, needCoreNum=ceil(10000/1024)=10
    string expectTilingData = "10000 10000 10 ";
    std::vector<size_t> expectWorkspaces = {YOLOX_BBOX_DECODE_SYS_WORKSPACE_SIZE};
    ExecuteTestCase(tilingContextPara, ge::GRAPH_SUCCESS, expectTilingKey, expectTilingData, expectWorkspaces);
}

// Test case 4: large shape, needCoreNum reaches core-limit 64
TEST_F(YoloxBoundingBoxDecodeTiling, yolox_bounding_box_decode_core_limit)
{
    struct YoloxBoundingBoxDecodeCompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "YoloxBoundingBoxDecode",
        {
            {{{100000, 4}, {100000, 4}}, ge::DT_FLOAT, ge::FORMAT_ND},         // input priors: (N, 4)
            {{{64, 100000, 4}, {64, 100000, 4}}, ge::DT_FLOAT, ge::FORMAT_ND}, // input bboxes: (B, N, 4)
        },
        {
            {{{64, 100000, 4}, {64, 100000, 4}}, ge::DT_FLOAT, ge::FORMAT_ND}, // output decoded_bboxes: (B, N, 4)
        },
        {
            /* attrs */
        },
        &compileInfo, "Ascend950",
        CORE_NUM, // number of cores obtained in the tiling phase
        UB_SIZE,  // the ubsize obtained in the tiling phase, but the actual obtained value is 256 bytes less than the
                  // specified value
        MAX_TILING_DATA_SIZE);    // specifies the maximum size of the tiling data in the tiling phase
    uint64_t expectTilingKey = 0; // YOLOX_BBOX_DECODE_SCH_MODE_BOX
    // boxCount=64*100000=6400000, boxNum=100000, perCoreBoxes=ceil(6400000/64)=100000, needCoreNum=min(64,64)=64
    string expectTilingData = "6400000 100000 64 ";
    std::vector<size_t> expectWorkspaces = {YOLOX_BBOX_DECODE_SYS_WORKSPACE_SIZE};
    ExecuteTestCase(tilingContextPara, ge::GRAPH_SUCCESS, expectTilingKey, expectTilingData, expectWorkspaces);
}

// Test case 5: boundary empty tensor, B=0, needCoreNum protected to 1
TEST_F(YoloxBoundingBoxDecodeTiling, yolox_bounding_box_decode_empty_batch)
{
    struct YoloxBoundingBoxDecodeCompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "YoloxBoundingBoxDecode",
        {
            {{{4, 4}, {4, 4}}, ge::DT_FLOAT, ge::FORMAT_ND},       // input priors: (N, 4)
            {{{0, 4, 4}, {0, 4, 4}}, ge::DT_FLOAT, ge::FORMAT_ND}, // input bboxes: (B, N, 4), B=0
        },
        {
            {{{0, 4, 4}, {0, 4, 4}}, ge::DT_FLOAT, ge::FORMAT_ND}, // output decoded_bboxes: (B, N, 4)
        },
        {
            /* attrs */
        },
        &compileInfo, "Ascend950",
        CORE_NUM, // number of cores obtained in the tiling phase
        UB_SIZE,  // the ubsize obtained in the tiling phase, but the actual obtained value is 256 bytes less than the
                  // specified value
        MAX_TILING_DATA_SIZE);    // specifies the maximum size of the tiling data in the tiling phase
    uint64_t expectTilingKey = 0; // YOLOX_BBOX_DECODE_SCH_MODE_BOX
    // boxCount=0*4=0, boxNum=4, needCoreNum=1 (empty tensor protection)
    string expectTilingData = "0 4 1 ";
    std::vector<size_t> expectWorkspaces = {YOLOX_BBOX_DECODE_SYS_WORKSPACE_SIZE};
    ExecuteTestCase(tilingContextPara, ge::GRAPH_SUCCESS, expectTilingKey, expectTilingData, expectWorkspaces);
}

// Test case 6: boundary empty tensor, N=0, needCoreNum protected to 1
TEST_F(YoloxBoundingBoxDecodeTiling, yolox_bounding_box_decode_empty_priors)
{
    struct YoloxBoundingBoxDecodeCompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "YoloxBoundingBoxDecode",
        {
            {{{0, 4}, {0, 4}}, ge::DT_FLOAT, ge::FORMAT_ND},       // input priors: (N, 4), N=0
            {{{2, 0, 4}, {2, 0, 4}}, ge::DT_FLOAT, ge::FORMAT_ND}, // input bboxes: (B, N, 4), N=0
        },
        {
            {{{2, 0, 4}, {2, 0, 4}}, ge::DT_FLOAT, ge::FORMAT_ND}, // output decoded_bboxes: (B, N, 4)
        },
        {
            /* attrs */
        },
        &compileInfo, "Ascend950",
        CORE_NUM, // number of cores obtained in the tiling phase
        UB_SIZE,  // the ubsize obtained in the tiling phase, but the actual obtained value is 256 bytes less than the
                  // specified value
        MAX_TILING_DATA_SIZE);    // specifies the maximum size of the tiling data in the tiling phase
    uint64_t expectTilingKey = 0; // YOLOX_BBOX_DECODE_SCH_MODE_BOX
    // boxCount=2*0=0, boxNum=0, needCoreNum=1 (empty tensor protection)
    string expectTilingData = "0 0 1 ";
    std::vector<size_t> expectWorkspaces = {YOLOX_BBOX_DECODE_SYS_WORKSPACE_SIZE};
    ExecuteTestCase(tilingContextPara, ge::GRAPH_SUCCESS, expectTilingKey, expectTilingData, expectWorkspaces);
}
