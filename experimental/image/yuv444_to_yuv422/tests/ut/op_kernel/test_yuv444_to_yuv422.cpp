/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file test_yuv444_to_yuv422.cpp
 * \brief
 */

#include "../../../op_kernel/yuv444_to_yuv422.cpp"
#include "yuv444_to_yuv422_tiling.h"
#include <array>
#include <vector>
#include <iostream>
#include <string>
#include <cstdint>
#include <cstdlib>
#include "gtest/gtest.h"
#include "tikicpulib.h"
#include "data_utils.h"

using namespace std;

class Yuv444ToYuv422Test : public testing::Test {
protected:
    static void SetUpTestCase()
    {
        cout << "Yuv444ToYuv422Test SetUp" << endl;
        const string cmd = "cp -rf " + dataPath + " ./";
        system(cmd.c_str());
        system("chmod -R 755 ./yuv444_to_yuv422_data/");
    }
    static void TearDownTestCase()
    {
        cout << "Yuv444ToYuv422Test TearDown" << endl;
    }
private:
    const static std::string rootPath;
    const static std::string dataPath;
};

const std::string Yuv444ToYuv422Test::rootPath = "../../../../";
const std::string Yuv444ToYuv422Test::dataPath = rootPath + "custom/yuv444_to_yuv422/tests/ut/op_kernel/yuv444_to_yuv422_data";

TEST_F(Yuv444ToYuv422Test, test_case_0)
{
    size_t h = 2, w = 4;
    size_t xByteSize = h * w * 4 * sizeof(uint16_t);
    size_t yByteSize = h * w * 2 * sizeof(uint8_t);
    size_t tiling_data_size = sizeof(Yuv444ToYuv422TilingData);
    uint32_t numBlocks = 1;

    system("cd ./yuv444_to_yuv422_data/ && python3 gen_data.py '(2, 4, 4)' 'float16'");
    std::string fileName = "./yuv444_to_yuv422_data/float16_input_yuv444_to_yuv422.bin";
    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    ReadFile(fileName, xByteSize, x, xByteSize);
    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(32);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);

    char* path_ = get_current_dir_name();
    string path(path_);

    Yuv444ToYuv422TilingData* tilingDatafromBin = reinterpret_cast<Yuv444ToYuv422TilingData*>(tiling);
    tilingDatafromBin->totalPairs = static_cast<int64_t>(h * (w / 2));
    tilingDatafromBin->h = static_cast<int64_t>(h);
    tilingDatafromBin->w = static_cast<int64_t>(w);
    tilingDatafromBin->needCoreNum = 1;

    ICPU_SET_TILING_KEY(0);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);

    ICPU_RUN_KF(yuv444_to_yuv422<0>,
        numBlocks,
        x,
        y,
        workspace,
        (uint8_t *)(tilingDatafromBin));

    fileName = "./yuv444_to_yuv422_data/float16_output_yuv444_to_yuv422.bin";
    WriteFile(fileName, y, yByteSize);

    AscendC::GmFree(x);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);

    system("cd ./yuv444_to_yuv422_data/ && python3 compare_data.py 'float16'");
    free(path_);
}
