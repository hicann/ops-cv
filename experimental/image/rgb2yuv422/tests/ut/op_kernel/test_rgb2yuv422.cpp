/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

#include "../../../op_kernel/rgb2yuv422.cpp"
#include "rgb2yuv422_tiling.h"
#include <array>
#include <vector>
#include <iostream>
#include <string>
#include <cstdint>
#include <cstdlib>
#include "gtest/gtest.h"
#include "tikicpulib.h"
#include "data_utils.h"

using namespace std;

class Rgb2yuv422Test : public testing::Test {
protected:
    static void SetUpTestCase()
    {
        cout << "Rgb2yuv422Test SetUp\n" << endl;
        const string cmd = "cp -rf " + dataPath + " ./";
        system(cmd.c_str());
        system("chmod -R 755 ./rgb2yuv422_data/");
    }
    static void TearDownTestCase()
    {
        cout << "Rgb2yuv422Test TearDown\n" << endl;
    }
private:
    const static std::string rootPath;
    const static std::string dataPath;
};

const std::string Rgb2yuv422Test::rootPath = "../../../../";
const std::string Rgb2yuv422Test::dataPath = rootPath + "custom/rgb2yuv422/tests/ut/op_kernel/rgb2yuv422_data";

TEST_F(Rgb2yuv422Test, test_case_float32_nhwc)
{
    size_t xByteSize = 4 * 8 * 3 * sizeof(float);
    size_t yByteSize = 4 * 8 * 2 * sizeof(float);
    size_t tiling_data_size = sizeof(Rgb2yuv422TilingData);
    uint32_t numBlocks = 1;

    int ret = system("cd ./rgb2yuv422_data/ && python3 gen_data.py '(4, 8, 3)' 'float32'");
    (void)ret;

    std::string fileName = "./rgb2yuv422_data/float32_input_rgb2yuv422.bin";
    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    ReadFile(fileName, xByteSize, x, xByteSize);
    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(32);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);

    char* path_ = get_current_dir_name();
    string path(path_);

    Rgb2yuv422TilingData* tilingDatafromBin = reinterpret_cast<Rgb2yuv422TilingData*>(tiling);
    tilingDatafromBin->needCoreNum = 1;
    tilingDatafromBin->totalRows = 4;
    tilingDatafromBin->perCoreRows = 4;
    tilingDatafromBin->W = 8;
    tilingDatafromBin->outerDims = 1;
    tilingDatafromBin->dataFormat = 0;
    tilingDatafromBin->pairsPerRow = 4;

    ICPU_SET_TILING_KEY(0);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);

    ICPU_RUN_KF(rgb2yuv422<0>,
        numBlocks,
        x,
        y,
        workspace,
        (uint8_t *)(tilingDatafromBin));

    fileName = "./rgb2yuv422_data/float32_output_rgb2yuv422.bin";
    WriteFile(fileName, y, yByteSize);

    AscendC::GmFree(x);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);

    system("cd ./rgb2yuv422_data/ && python3 compare_data.py 'float32'");
    free(path_);
}
