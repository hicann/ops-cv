/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/*!
 * \file dilation2_d_backprop_input_def.cpp
 * \brief Operator definition for dilation2_d_backprop_input operator
 */
/* Generated By CANNBot */

#include "register/op_def_registry.h"

namespace ops {
class Dilation2DBackpropInput : public OpDef {
public:
    explicit Dilation2DBackpropInput(const char* name) : OpDef(name)
    {
        // 输入 x: 4D (N,H,W,C) 或 (N,C,H,W), float32, ND
        this->Input("x")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();

        // 输入 filter: 3D, dtype 与 x 相同
        this->Input("filter")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();

        // 输入 out_backprop: 4D, dtype 与 x 相同
        this->Input("out_backprop")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();

        // 输出 y: 4D, dtype 与 x 相同, shape = x shape
        this->Output("y")
            .ParamType(REQUIRED)
            .DataType({ge::DT_FLOAT})
            .Format({ge::FORMAT_ND})
            .UnknownShapeFormat({ge::FORMAT_ND})
            .AutoContiguous();

        // 属性: SE §6.2 REG_OP 定义
        this->Attr("strides").AttrType(REQUIRED).ListInt();
        this->Attr("rates").AttrType(REQUIRED).ListInt();
        this->Attr("padding_mode").AttrType(OPTIONAL).String("SAME");
        this->Attr("pads").AttrType(OPTIONAL).ListInt({0, 0, 0, 0});
        this->Attr("ceil_mode").AttrType(OPTIONAL).Bool(false);
        this->Attr("data_format").AttrType(OPTIONAL).String("NHWC");

        OpAICoreConfig aicoreConfig;
        aicoreConfig.DynamicCompileStaticFlag(true)
            .DynamicFormatFlag(false)
            .DynamicRankSupportFlag(true)
            .DynamicShapeSupportFlag(true)
            .NeedCheckSupportFlag(false)
            .PrecisionReduceFlag(true)
            .ExtendCfgInfo("opFile.value", "dilation2_d_backprop_input_apt");
        this->AICore().AddConfig("ascend950", aicoreConfig);
    }
};
OP_ADD(Dilation2DBackpropInput);
} // namespace ops
