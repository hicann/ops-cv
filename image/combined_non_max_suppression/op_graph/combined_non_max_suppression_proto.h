/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file combined_non_max_suppression_proto.h
 * \brief Operator proto declaration for CombinedNonMaxSuppression.
 */
#ifndef OPS_OP_PROTO_INC_COMBINED_NON_MAX_SUPPRESSION_H_
#define OPS_OP_PROTO_INC_COMBINED_NON_MAX_SUPPRESSION_H_

#include "graph/operator_reg.h"
#include "graph/types.h"

namespace ge {

/**
 * @brief Performs greedy non-maximum suppression independently for each class and batch.
 *
 * @par Inputs:
 * @li boxes: A 4D float Tensor of shape [batch, num_boxes, q, 4], where q is 1 or num_classes.
 * @li scores: A 3D float Tensor of shape [batch, num_boxes, num_classes].
 * @li max_output_size_per_class: A scalar int32 Tensor specifying the maximum output size per class.
 * @li max_total_size: A scalar int32 Tensor specifying the maximum total output size per batch.
 * @li iou_threshold: A scalar float Tensor specifying the IoU suppression threshold.
 * @li score_threshold: A scalar float Tensor specifying the score threshold.
 *
 * @par Attributes:
 * @li pad_per_class: If true, pad the output size per class. Defaults to false.
 * @li clip_boxes: If true, clip output box coordinates to [0, 1]. Defaults to true.
 *
 * @par Outputs:
 * @li nmsed_boxes: A 3D float Tensor containing selected boxes.
 * @li nmsed_scores: A 2D float Tensor containing selected scores.
 * @li nmsed_classes: A 2D float Tensor containing selected class indices.
 * @li valid_detections: A 1D int32 Tensor containing valid detection counts.
 *
 * @par Third-party framework compatibility
 * Compatible with the TensorFlow CombinedNonMaxSuppression operator.
 */
REG_OP(CombinedNonMaxSuppression)
    .INPUT(boxes, TensorType({DT_FLOAT}))
    .INPUT(scores, TensorType({DT_FLOAT}))
    .INPUT(max_output_size_per_class, TensorType({DT_INT32}))
    .INPUT(max_total_size, TensorType({DT_INT32}))
    .INPUT(iou_threshold, TensorType({DT_FLOAT}))
    .INPUT(score_threshold, TensorType({DT_FLOAT}))
    .OUTPUT(nmsed_boxes, TensorType({DT_FLOAT}))
    .OUTPUT(nmsed_scores, TensorType({DT_FLOAT}))
    .OUTPUT(nmsed_classes, TensorType({DT_FLOAT}))
    .OUTPUT(valid_detections, TensorType({DT_INT32}))
    .ATTR(pad_per_class, Bool, false)
    .ATTR(clip_boxes, Bool, true)
    .OP_END_FACTORY_REG(CombinedNonMaxSuppression)

} // namespace ge

#endif // OPS_OP_PROTO_INC_COMBINED_NON_MAX_SUPPRESSION_H_
