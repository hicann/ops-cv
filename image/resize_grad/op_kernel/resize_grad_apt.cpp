/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file resize_grad_apt.cpp
 * \brief resize grad apt
 */

#include "arch35/resize_grad_simt.h"

extern "C" __global__ __aicore__ void resize_grad(GM_ADDR grads, GM_ADDR roi, GM_ADDR scales, GM_ADDR original_size,
                                                  GM_ADDR y, GM_ADDR workspace, GM_ADDR tiling)
{
    KERNEL_TASK_TYPE_DEFAULT(KERNEL_TYPE_AIV_ONLY);
    REGISTER_TILING_DEFAULT(ResizeGradTilingData);
    GET_TILING_DATA_WITH_STRUCT(ResizeGradTilingData, tilingDataIn, tiling);

    if (TILING_KEY_IS(TILING_KEY_COPY)) { // 同尺寸纯拷贝，idx32
        NsResizeGrad::ProcessCopy<DTYPE_GRADS, uint32_t>(grads, y, &tilingDataIn);
        return;
    }
    if (TILING_KEY_IS(TILING_KEY_COPY_IDX64)) { // 同尺寸纯拷贝，idx64
        NsResizeGrad::ProcessCopy<DTYPE_GRADS, uint64_t>(grads, y, &tilingDataIn);
        return;
    }
    if (TILING_KEY_IS(TILING_KEY_CUBIC_DETERMINE)) {
        NsResizeGrad::DispatchCubic<DTYPE_GRADS, uint32_t, int32_t>(grads, y, &tilingDataIn);
        return;
    }
    if (TILING_KEY_IS(TILING_KEY_CUBIC_DETERMINE_IDX64)) {
        NsResizeGrad::DispatchCubic<DTYPE_GRADS, uint64_t, int64_t>(grads, y, &tilingDataIn);
        return;
    }
    if (TILING_KEY_IS(TILING_KEY_LINEAR_DETERMINE)) {
        NsResizeGrad::DispatchLinear<DTYPE_GRADS, uint32_t, int32_t>(grads, y, &tilingDataIn);
        return;
    }
    if (TILING_KEY_IS(TILING_KEY_LINEAR_DETERMINE_IDX64)) {
        NsResizeGrad::DispatchLinear<DTYPE_GRADS, uint64_t, int64_t>(grads, y, &tilingDataIn);
        return;
    }
}
