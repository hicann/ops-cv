/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file resize_grad_proto.h
 * \brief resize grad proto
 */
#ifndef OPS_OP_PROTO_INC_RESIZEGRAD_H_
#define OPS_OP_PROTO_INC_RESIZEGRAD_H_

#include "graph/operator_reg.h"
#include "graph/types.h"

namespace ge {

/**
*@brief Computes the gradient of the Resize operation.
*@par Inputs:
*Four inputs, including:
* @li grads: A 4D Tensor of gradients from the downstream operation.
*     Must be one of the following types: float16, float32.
* @li roi: An optional 1D Tensor of region-of-interest values.
*     Must be one of the following types: float16, float32, double.
* @li scales: An optional 1D Tensor of scale factors. Must be float32.
* @li original_size: A required 1D Tensor holding the shape of the original
*     (forward-input) tensor. Must be one of the following types: int32, int64. \n

*@par Attributes:
*Seven attributes, including:
* @li coordinate_transformation_mode: String. Defaults to "half_pixel".
* @li cubic_coeff_a: Float. Defaults to -0.75.
* @li exclude_outside: Int. Defaults to 0.
* @li extrapolation_value: Float. Defaults to 0.0.
* @li mode: String. Defaults to "nearest".
* @li nearest_mode: String. Defaults to "round_prefer_floor".
* @li data_format: String. Defaults to "NCHW". \n

*@par Outputs:
*y: A 4D Tensor with the same type as grads, shape depends on grads and
*original_size.
*/
#ifndef OPS_PROTO_DEF_RESIZEGRAD
#define OPS_PROTO_DEF_RESIZEGRAD
REG_OP(ResizeGrad)
    .INPUT(grads, TensorType({OrdinaryType, DT_STRING}))
    .OPTIONAL_INPUT(roi, TensorType({DT_FLOAT16, DT_FLOAT, DT_DOUBLE}))
    .OPTIONAL_INPUT(scales, TensorType({DT_FLOAT}))
    .INPUT(original_size, TensorType({DT_INT64, DT_INT32}))
    .OUTPUT(y, TensorType({OrdinaryType, DT_STRING}))
    .ATTR(coordinate_transformation_mode, String, "half_pixel")
    .ATTR(cubic_coeff_a, Float, -0.75)
    .ATTR(exclude_outside, Int, 0)
    .ATTR(extrapolation_value, Float, 0.0)
    .ATTR(mode, String, "nearest")
    .ATTR(nearest_mode, String, "round_prefer_floor")
    .ATTR(data_format, String, "NCHW")
    .OP_END_FACTORY_REG(ResizeGrad)
#endif

} // namespace ge

#endif
