/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file test_col2_im_v2.cpp
 * \brief
 */

#include "../../../op_kernel/col2_im_v2_apt.cpp"
#include "col2_im_v2_tiling.h"
#include <array>
#include <vector>
#include <iostream>
#include <string>
#include <cstdint>
#include <cstdlib>
#include "gtest/gtest.h"
#include "tikicpulib.h"
#include "data_utils.h"

using namespace std;

class Col2ImV2Test : public testing::Test {
protected:
    static void SetUpTestCase()
    {
        cout << "Col2ImV2Test SetUp\n" << endl;
        const string cmd = "cp -rf " + dataPath + " ./";
        system(cmd.c_str());
        system("chmod -R 755 ./col2_im_v2_data/");
    }
    static void TearDownTestCase() { cout << "Col2ImV2Test TearDown\n" << endl; }

private:
    const static std::string rootPath;
    const static std::string dataPath;
};

const std::string Col2ImV2Test::rootPath = "../../../../";
const std::string Col2ImV2Test::dataPath = rootPath + "custom/col2_im_v2/tests/ut/op_kernel/col2_im_v2_data";

TEST_F(Col2ImV2Test, test_case_0)
{
    // n=1, c=1, outH=outW=4, kH=kW=2, dilation=(1,1), padding=(0,0), stride=(1,1)
    // ho=wo=3, x: (1, 4, 9), y: (1, 1, 4, 4)
    size_t xByteSize = 1 * 4 * 9 * sizeof(float);
    size_t outSizeByteSize = 2 * sizeof(int32_t);
    size_t kSizeByteSize = 2 * sizeof(int32_t);
    size_t yByteSize = 1 * 1 * 4 * 4 * sizeof(float);
    size_t tiling_data_size = sizeof(Col2ImV2TilingData);
    uint32_t numBlocks = 1;

    system("cd ./col2_im_v2_data/ && python3 gen_data.py '(1, 4, 9)' 'float32' 4 4 2 2 1 1 0 0 1 1");

    std::string fileName = "./col2_im_v2_data/float32_input_col2_im_v2.bin";
    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    ReadFile(fileName, xByteSize, x, xByteSize);

    fileName = "./col2_im_v2_data/float32_input_outsize.bin";
    uint8_t* output_size = (uint8_t*)AscendC::GmAlloc(outSizeByteSize);
    ReadFile(fileName, outSizeByteSize, output_size, outSizeByteSize);

    fileName = "./col2_im_v2_data/float32_input_ksize.bin";
    uint8_t* kernel_size = (uint8_t*)AscendC::GmAlloc(kSizeByteSize);
    ReadFile(fileName, kSizeByteSize, kernel_size, kSizeByteSize);

    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(32);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);

    char* path_ = get_current_dir_name();
    string path(path_);

    Col2ImV2TilingData* tilingDatafromBin = reinterpret_cast<Col2ImV2TilingData*>(tiling);
    tilingDatafromBin->totalLength = 1 * 1 * 4 * 4;
    tilingDatafromBin->needCoreNum = 1;
    tilingDatafromBin->outputSizeH = 4;
    tilingDatafromBin->outputSizeW = 4;
    tilingDatafromBin->kernelSizeH = 2;
    tilingDatafromBin->kernelSizeW = 2;
    tilingDatafromBin->dilationH = 1;
    tilingDatafromBin->dilationW = 1;
    tilingDatafromBin->paddingH = 0;
    tilingDatafromBin->paddingW = 0;
    tilingDatafromBin->strideH = 1;
    tilingDatafromBin->strideW = 1;
    tilingDatafromBin->colH = 3;
    tilingDatafromBin->colW = 3;

    ICPU_SET_TILING_KEY(0);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);

    ICPU_RUN_KF(col2_im_v2<0>, numBlocks, x, output_size, kernel_size, y, workspace, (uint8_t*)(tilingDatafromBin));

    fileName = "./col2_im_v2_data/float32_output_col2_im_v2.bin";
    WriteFile(fileName, y, yByteSize);

    // 释放资源
    AscendC::GmFree(x);
    AscendC::GmFree(output_size);
    AscendC::GmFree(kernel_size);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);

    system("cd ./col2_im_v2_data/ && python3 compare_data.py 'float32'");
    free(path_);
}

TEST_F(Col2ImV2Test, test_case_1)
{
    // n=2, c=2, outH=outW=8, kH=kW=3, dilation=(1,1), padding=(1,1), stride=(2,2)
    // ho=wo=4, x: (2, 18, 16), y: (2, 2, 8, 8)
    size_t xByteSize = 2 * 18 * 16 * sizeof(float);
    size_t outSizeByteSize = 2 * sizeof(int32_t);
    size_t kSizeByteSize = 2 * sizeof(int32_t);
    size_t yByteSize = 2 * 2 * 8 * 8 * sizeof(float);
    size_t tiling_data_size = sizeof(Col2ImV2TilingData);
    uint32_t numBlocks = 1;

    system("cd ./col2_im_v2_data/ && python3 gen_data.py '(2, 18, 16)' 'float32' 8 8 3 3 1 1 1 1 2 2");

    std::string fileName = "./col2_im_v2_data/float32_input_col2_im_v2.bin";
    uint8_t* x = (uint8_t*)AscendC::GmAlloc(xByteSize);
    ReadFile(fileName, xByteSize, x, xByteSize);

    fileName = "./col2_im_v2_data/float32_input_outsize.bin";
    uint8_t* output_size = (uint8_t*)AscendC::GmAlloc(outSizeByteSize);
    ReadFile(fileName, outSizeByteSize, output_size, outSizeByteSize);

    fileName = "./col2_im_v2_data/float32_input_ksize.bin";
    uint8_t* kernel_size = (uint8_t*)AscendC::GmAlloc(kSizeByteSize);
    ReadFile(fileName, kSizeByteSize, kernel_size, kSizeByteSize);

    uint8_t* y = (uint8_t*)AscendC::GmAlloc(yByteSize);
    uint8_t* workspace = (uint8_t*)AscendC::GmAlloc(32);
    uint8_t* tiling = (uint8_t*)AscendC::GmAlloc(tiling_data_size);

    char* path_ = get_current_dir_name();
    string path(path_);

    Col2ImV2TilingData* tilingDatafromBin = reinterpret_cast<Col2ImV2TilingData*>(tiling);
    tilingDatafromBin->totalLength = 2 * 2 * 8 * 8;
    tilingDatafromBin->needCoreNum = 1;
    tilingDatafromBin->outputSizeH = 8;
    tilingDatafromBin->outputSizeW = 8;
    tilingDatafromBin->kernelSizeH = 3;
    tilingDatafromBin->kernelSizeW = 3;
    tilingDatafromBin->dilationH = 1;
    tilingDatafromBin->dilationW = 1;
    tilingDatafromBin->paddingH = 1;
    tilingDatafromBin->paddingW = 1;
    tilingDatafromBin->strideH = 2;
    tilingDatafromBin->strideW = 2;
    tilingDatafromBin->colH = 4;
    tilingDatafromBin->colW = 4;

    ICPU_SET_TILING_KEY(0);
    AscendC::SetKernelMode(KernelMode::AIV_MODE);

    ICPU_RUN_KF(col2_im_v2<0>, numBlocks, x, output_size, kernel_size, y, workspace, (uint8_t*)(tilingDatafromBin));

    fileName = "./col2_im_v2_data/float32_output_col2_im_v2.bin";
    WriteFile(fileName, y, yByteSize);

    // 释放资源
    AscendC::GmFree(x);
    AscendC::GmFree(output_size);
    AscendC::GmFree(kernel_size);
    AscendC::GmFree(y);
    AscendC::GmFree(workspace);
    AscendC::GmFree(tiling);

    system("cd ./col2_im_v2_data/ && python3 compare_data.py 'float32'");
    free(path_);
}
