/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file extract_image_patches_tiling_data.h
 * \brief Tiling data struct for extract_image_patches operator
 *
 * NHWC 和 NCHW 路径共用同一个 TilingData 结构体，各路径取所需字段。
 * 不包含 threadNum 等 VF 线程数参数（编译期常量）。
 */

#ifndef EXTRACT_IMAGE_PATCHES_TILING_DATA_H_
#define EXTRACT_IMAGE_PATCHES_TILING_DATA_H_

// Uniform field types keep Host and Kernel layouts consistent.
struct ExtractImagePatchesTilingData {
    int64_t needCoreNum = 0;   // 实际启动核数
    int64_t totalElements = 0; // 输出总元素数 = N * out_h * out_w * out_c
    // —— 坐标分解除数（NHWC 路径）——
    int64_t divNhc = 0; // out_h * out_w * out_c（n 反推除数）
    int64_t divWc = 0;  // out_w * out_c（i 反推除数）
    // —— 坐标分解除数（NCHW 路径）——
    int64_t divNcHw = 0; // out_c * out_h * out_w（n 反推除数）
    int64_t divHw = 0;   // out_h * out_w（oc 反推除数）
    // —— 公用维度参数 ——
    int64_t outC = 0; // out_c = inC * kH * kW
    int64_t outW = 0; // out_w
    int64_t khKw = 0; // kH * kW（c 反推除数）
    int64_t kW = 0;   // p 反推除数
    // —— 输入空间维度（边界检查）——
    int64_t inH = 0; // 输入 H
    int64_t inW = 0; // 输入 W
    // —— 滑动窗口参数 ——
    int64_t strideH = 0;  // H 方向步长
    int64_t strideW = 0;  // W 方向步长
    int64_t rateH = 0;    // H 方向扩张率（dilation）
    int64_t rateW = 0;    // W 方向扩张率
    int64_t padHTop = 0;  // H 方向上 padding（SAME: pad_total/2; VALID: 0）
    int64_t padWLeft = 0; // W 方向左 padding
    // —— 输入物理步长（逻辑 N/H/W/C 维在 GM 中的步长）——
    int64_t inStrideN = 0; // NHWC: H*W*C;  NCHW: C*H*W
    int64_t inStrideH = 0; // NHWC: W*C;    NCHW: W
    int64_t inStrideW = 0; // NHWC: C;      NCHW: 1
    int64_t inStrideC = 0; // NHWC: 1;      NCHW: H*W
    // —— 多核分块参数（select 模式：Host 侧 Process() 用 GetBlockIdx 切分）——
    int64_t perCoreElements = 0;  // 每核元素数（对齐到 32）
    int64_t lastCoreElements = 0; // 最后一个核的元素数
};

#endif // EXTRACT_IMAGE_PATCHES_TILING_DATA_H_
