/**
 * Copyright (c) 2026 Huawei Technologies Co., Ltd.
 * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
 * CANN Open Software License Agreement Version 2.0 (the "License").
 * Please refer to the License for details. You may not use this file except in compliance with the License.
 * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
 * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
 * See LICENSE in the root of the software repository for the full text of the License.
 */

/* Generated By CANNBot */

/*!
 * \file test_points_in_polygons_tiling.cpp
 * \brief PointsInPolygons tiling UT: 覆盖 EMPTY(KEY=0) / NORMAL(KEY=1) / N_VEC(KEY=2) 三分支
 *        平台参数: coreNum=48, ubSize=253952(ascend950 arch35)
 *        输入契约: points=(N,2), polygons=(8,M)；NORMAL 分支 UB 反推 tileM=32, tileN=240
 */

#include <iostream>
#include <gtest/gtest.h>
#include "tiling_context_faker.h"
#include "tiling_case_executor.h"
#include "../../../../op_kernel/arch35/points_in_polygons_tiling_data.h"

using namespace std;
using namespace ge;

class PointsInPolygonsTiling : public testing::Test {
protected:
    static void SetUpTestCase() { std::cout << "PointsInPolygonsTiling SetUp" << std::endl; }

    static void TearDownTestCase() { std::cout << "PointsInPolygonsTiling TearDown" << std::endl; }
};

// Branch-0 (EMPTY): N=0 → tilingKey=0，全 0 哨兵
TEST_F(PointsInPolygonsTiling, tiling_case0_empty_n)
{
    struct CompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "PointsInPolygons",
        {
            {{{0, 2}, {0, 2}}, ge::DT_FLOAT, ge::FORMAT_ND}, // points (N=0)
            {{{8, 4}, {8, 4}}, ge::DT_FLOAT, ge::FORMAT_ND}, // polygons (8,M), M=4
        },
        {
            {{{0, 4}, {0, 4}}, ge::DT_FLOAT, ge::FORMAT_ND}, // output (0,4)
        },
        {}, &compileInfo, "Ascend950", 48, 253952, 4096);
    TilingInfo tilingInfo;
    bool tilingOk = ExecuteTiling(tilingContextPara, tilingInfo);
    EXPECT_TRUE(tilingOk);
    EXPECT_EQ(tilingInfo.tilingKey, 0);

    ASSERT_GE(tilingInfo.tilingDataSize, sizeof(PointsInPolygonsTilingData));
    const PointsInPolygonsTilingData* td = reinterpret_cast<const PointsInPolygonsTilingData*>(
        tilingInfo.tilingData.get());
    // outN=0, outM=4
    EXPECT_EQ(td->outN, 0);
    EXPECT_EQ(td->outM, 4);
    EXPECT_EQ(td->totalTiles, 0u);
    EXPECT_EQ(td->perCoreCount, 0u);
    EXPECT_EQ(td->realCoreNum, 0u);
    EXPECT_EQ(td->bufferSize, 0u);
}

// Branch-0 (EMPTY): M=0 → tilingKey=0
TEST_F(PointsInPolygonsTiling, tiling_case1_empty_m)
{
    struct CompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "PointsInPolygons",
        {
            {{{8, 2}, {8, 2}}, ge::DT_FLOAT, ge::FORMAT_ND},
            {{{8, 0}, {8, 0}}, ge::DT_FLOAT, ge::FORMAT_ND}, // polygons (8,0), M=0
        },
        {
            {{{8, 0}, {8, 0}}, ge::DT_FLOAT, ge::FORMAT_ND},
        },
        {}, &compileInfo, "Ascend950", 48, 253952, 4096);
    TilingInfo tilingInfo;
    bool tilingOk = ExecuteTiling(tilingContextPara, tilingInfo);
    EXPECT_TRUE(tilingOk);
    EXPECT_EQ(tilingInfo.tilingKey, 0);

    ASSERT_GE(tilingInfo.tilingDataSize, sizeof(PointsInPolygonsTilingData));
    const PointsInPolygonsTilingData* td = reinterpret_cast<const PointsInPolygonsTilingData*>(
        tilingInfo.tilingData.get());
    EXPECT_EQ(td->outN, 8);
    EXPECT_EQ(td->outM, 0);
    EXPECT_EQ(td->totalTiles, 0u);
    EXPECT_EQ(td->bufferSize, 0u);
}

// Branch-1 (NORMAL): N=4, M=2 → tilingKey=1
TEST_F(PointsInPolygonsTiling, tiling_case2_normal_small)
{
    struct CompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "PointsInPolygons",
        {
            {{{4, 2}, {4, 2}}, ge::DT_FLOAT, ge::FORMAT_ND}, // points
            {{{8, 2}, {8, 2}}, ge::DT_FLOAT, ge::FORMAT_ND}, // polygons (8,M), M=2
        },
        {
            {{{4, 2}, {4, 2}}, ge::DT_FLOAT, ge::FORMAT_ND}, // output
        },
        {}, &compileInfo, "Ascend950", 48, 253952, 4096);
    TilingInfo tilingInfo;
    bool tilingOk = ExecuteTiling(tilingContextPara, tilingInfo);
    EXPECT_TRUE(tilingOk);
    EXPECT_EQ(tilingInfo.tilingKey, 1);

    ASSERT_GE(tilingInfo.tilingDataSize, sizeof(PointsInPolygonsTilingData));
    const PointsInPolygonsTilingData* td = reinterpret_cast<const PointsInPolygonsTilingData*>(
        tilingInfo.tilingData.get());
    EXPECT_EQ(td->outN, 4);
    EXPECT_EQ(td->outM, 2);
    // UB 反推: tileM=32, tileN=240
    EXPECT_EQ(td->tileN, 240);
    EXPECT_EQ(td->tileM, 32);
    EXPECT_EQ(td->tileNTail, 4);
    EXPECT_EQ(td->tileMTail, 2);
    EXPECT_EQ(td->numTilesN, 1);
    EXPECT_EQ(td->numTilesM, 1);
    EXPECT_EQ(td->totalTiles, 1u);
    EXPECT_EQ(td->perCoreCount, 1u);
    EXPECT_EQ(td->realCoreNum, 1u);
    EXPECT_EQ(td->bufferSize, 30720u);
}

// Branch-1 (NORMAL): N=1024, M=64 → tilingKey=1，多 tile 多核
TEST_F(PointsInPolygonsTiling, tiling_case3_normal_large)
{
    struct CompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "PointsInPolygons",
        {
            {{{1024, 2}, {1024, 2}}, ge::DT_FLOAT, ge::FORMAT_ND},
            {{{8, 64}, {8, 64}}, ge::DT_FLOAT, ge::FORMAT_ND}, // polygons (8,M), M=64
        },
        {
            {{{1024, 64}, {1024, 64}}, ge::DT_FLOAT, ge::FORMAT_ND},
        },
        {}, &compileInfo, "Ascend950", 48, 253952, 4096);
    TilingInfo tilingInfo;
    bool tilingOk = ExecuteTiling(tilingContextPara, tilingInfo);
    EXPECT_TRUE(tilingOk);
    EXPECT_EQ(tilingInfo.tilingKey, 1);

    ASSERT_GE(tilingInfo.tilingDataSize, sizeof(PointsInPolygonsTilingData));
    const PointsInPolygonsTilingData* td = reinterpret_cast<const PointsInPolygonsTilingData*>(
        tilingInfo.tilingData.get());
    EXPECT_EQ(td->outN, 1024);
    EXPECT_EQ(td->outM, 64);
    EXPECT_EQ(td->tileN, 240);
    EXPECT_EQ(td->tileM, 32);
    EXPECT_EQ(td->tileNTail, 64);
    EXPECT_EQ(td->tileMTail, 0);
    EXPECT_EQ(td->numTilesN, 5);
    EXPECT_EQ(td->numTilesM, 2);
    EXPECT_EQ(td->totalTiles, 10u);
    EXPECT_EQ(td->perCoreCount, 1u);
    EXPECT_EQ(td->realCoreNum, 10u);
    EXPECT_EQ(td->bufferSize, 30720u);
}

// Branch-2 (N_VEC): N=256, M=2 → tilingKey=2 (M<=16 且 N>=256)
TEST_F(PointsInPolygonsTiling, tiling_case4_nvec)
{
    struct CompileInfo {
    } compileInfo;
    gert::TilingContextPara tilingContextPara(
        "PointsInPolygons",
        {
            {{{256, 2}, {256, 2}}, ge::DT_FLOAT, ge::FORMAT_ND},
            {{{8, 2}, {8, 2}}, ge::DT_FLOAT, ge::FORMAT_ND}, // polygons (8,M), M=2
        },
        {
            {{{256, 2}, {256, 2}}, ge::DT_FLOAT, ge::FORMAT_ND},
        },
        {}, &compileInfo, "Ascend950", 48, 253952, 4096);
    TilingInfo tilingInfo;
    bool tilingOk = ExecuteTiling(tilingContextPara, tilingInfo);
    EXPECT_TRUE(tilingOk);
    EXPECT_EQ(tilingInfo.tilingKey, 2);

    ASSERT_GE(tilingInfo.tilingDataSize, sizeof(PointsInPolygonsTilingData));
    const PointsInPolygonsTilingData* td = reinterpret_cast<const PointsInPolygonsTilingData*>(
        tilingInfo.tilingData.get());
    EXPECT_EQ(td->outN, 256);
    EXPECT_EQ(td->outM, 2);
    EXPECT_EQ(td->tileN, 0);
    EXPECT_EQ(td->tileM, 0);
    EXPECT_EQ(td->tileNVec, 7928);
    EXPECT_EQ(td->numTilesNVec, 1);
    EXPECT_EQ(td->totalTiles, 1u);
    EXPECT_EQ(td->perCoreCount, 1u);
    EXPECT_EQ(td->realCoreNum, 1u);
    EXPECT_EQ(td->bufferSize, 63424u);
}
